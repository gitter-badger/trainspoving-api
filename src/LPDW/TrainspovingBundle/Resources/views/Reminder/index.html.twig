{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Reminder list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Arrivaldate</th>
                <th>Leeway</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('reminder_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.arrivalDate %}{{ entity.arrivalDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.leeway }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('reminder_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('reminder_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('reminder_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
